# Copie para .env e ajuste as portas se 3000/3001 estiverem em uso.
# Backend (NestJS) — porta local e porta publicada no Docker.
BACKEND_PORT=3001
# Frontend (Vite em dev, nginx no Docker) — porta local e porta publicada no Docker.
FRONTEND_PORT=3000
