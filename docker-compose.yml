services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - finfin-data:/app/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  finfin-data:
